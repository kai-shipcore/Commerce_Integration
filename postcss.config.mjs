/**
 * Code Guide:
 * PostCSS configuration used by the styling pipeline.
 * Tailwind and related CSS transforms are wired in here.
 */

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
